#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--)
    {
        int s;
        cin >> s;
        int maximumProduct = -(s + 1);
        cout << maximumProduct << endl;
    }
}